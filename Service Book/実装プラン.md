# Service Book 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 給湯機器の「顧客 ＞ 設置機器 ＞ サービス記録」を蓄積し、新規依頼の発行（依頼票A4）と完了報告書（サービス報告書A4）を1ツールで発行できるブラウザ完結型台帳を作る。

**Architecture:** 既存「BtoB価格表」ツールの構成を踏襲。IndexedDB を主ストアにし、`data/` フォルダの JSON へ Directory Access API で自動同期。単一HTML＋タブ型ページ切替（`.page` / `.nav-item`）、モーダルCRUD、`showToast`/`escHtml`/`lucide` を共通利用。半角全角の自動正規化と横断あいまい検索を独立ユーティリティ `normalize.js` に切り出す。

**Tech Stack:** Vanilla HTML/CSS/JS、IndexedDB、File System Access API（`showDirectoryPicker`）、Lucide Icons（CDN）。テストランナーは導入しない（プロジェクト方針＝ブラウザ手動検証＋純粋関数はコンソール自己テスト）。

**設計の出典:** [設計プラン.md](設計プラン.md) / 参照実装: [../BtoB Price data base/js/app.js](../BtoB%20Price%20data%20base/js/app.js), [../BtoB Price data base/js/db.js](../BtoB%20Price%20data%20base/js/db.js)

---

## ファイル構成

```
Service Book/
├── index.html        … タブUI骨格・ページ・モーダル・トースト・CDN読み込み
├── css/style.css     … BtoBのstyle.cssをベースに流用
├── js/
│   ├── normalize.js  … 半角全角正規化＋あいまい検索（純粋関数）
│   ├── db.js         … IndexedDB（customers/equipments/serviceRecords/appSettings）＋汎用CRUD
│   └── app.js        … 画面ロジック・フォルダ同期・印刷生成
└── data/             … 同期先（実行時に自動生成）
    ├── customers.json
    ├── equipments.json
    └── service_records.json
```

**読み込み順（index.html末尾）:** `normalize.js` → `db.js` → `app.js`。

各ファイル先頭に必ず `/* © 2026 Nozomi Sakurada. All rights reserved. */` を付ける。

---

## データ型（全タスク共通の契約）

```
customer       = { id, name, kana, contact, address, dealerName, houseMaker, printNote, memo }
equipment      = { id, customerId, maker, modelNo, lotNo, category, gasType, installDate, usage, memo }
serviceRecord  = {
  id, equipmentId, mgmtNo, receivedDate, receptionist, requestedDate,
  serviceDate, completionDate, staff, callContent, workType,
  parts:[{name, qty, unitPrice}], fee:{visitFee, laborFee, partsFee, total},
  remarks, customerReport, status:'open'|'completed', createdAt, updatedAt
}
```

- 日付はすべて `YYYY-MM-DD` 文字列。`createdAt`/`updatedAt` も `YYYY-MM-DD`。
- `status` は `completionDate` 入力で `'completed'`、未入力なら `'open'`。
- 金額は数値。`fee.total = visitFee + laborFee + partsFee`（部品代は parts 合計と一致させる）。

---

## Task 0: スキャフォールド（骨格・CSS・CDN）

**Files:**
- Create: `Service Book/index.html`
- Create: `Service Book/css/style.css`
- Create: `Service Book/js/normalize.js`（空＋著作権ヘッダ）
- Create: `Service Book/js/db.js`（空＋著作権ヘッダ）
- Create: `Service Book/js/app.js`（最小・ナビゲーションのみ）

- [ ] **Step 1: CSSを流用**

[../BtoB Price data base/css/style.css](../BtoB%20Price%20data%20base/css/style.css) の内容をそのまま `Service Book/css/style.css` にコピーする（`.page`/`.nav-item`/`.btn`/`.badge`/`.modal`/`.toast`/CSS変数 `--info`/`--success`/`--danger`/`--text3`/`--border` 等をそのまま使うため）。先頭の著作権ヘッダは維持する。

- [ ] **Step 2: index.html 骨格を作成**

5つのナビ項目とページ、トースト、モーダル置き場を用意する。`<head>` に Lucide CDN を含める。

```html
<!-- © 2026 Nozomi Sakurada. All rights reserved. -->
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>サービス報告書・点検履歴台帳 | Construction Field Hub</title>
  <link rel="stylesheet" href="css/style.css">
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>
  <header class="app-header">
    <div class="top-label-en">Service Book</div>
    <h1>サービス報告書・点検履歴台帳</h1>
    <div id="folder-disconnected-banner" class="banner" style="display:none">
      <span>dataフォルダ未接続</span>
      <button class="btn btn-sm btn-primary" type="button" onclick="connectDataFolder()">フォルダを接続</button>
    </div>
    <div id="folder-connected-banner" class="banner" style="display:none">
      <span>同期中: <strong id="folder-connected-name">—</strong></span>
      <button class="btn btn-sm btn-secondary" type="button" onclick="disconnectDataFolder()">解除</button>
    </div>
    <div id="global-sync-alert" class="banner" style="display:none">
      <span>フォルダの再接続が必要です</span>
      <button class="btn btn-sm btn-primary" type="button" onclick="reconnectAllFiles()">再接続</button>
    </div>
  </header>

  <nav class="nav">
    <button class="nav-item active" data-page="intake" type="button">新規依頼発行</button>
    <button class="nav-item" data-page="customers" type="button">顧客管理</button>
    <button class="nav-item" data-page="equipments" type="button">機器管理</button>
    <button class="nav-item" data-page="records" type="button">サービス記録 <span id="nav-badge-stale" class="badge badge-expired" style="display:none">0</span></button>
    <button class="nav-item" data-page="print" type="button">報告書印刷</button>
  </nav>

  <main>
    <section id="page-intake" class="page active"></section>
    <section id="page-customers" class="page"></section>
    <section id="page-equipments" class="page"></section>
    <section id="page-records" class="page"></section>
    <section id="page-print" class="page"></section>
  </main>

  <div id="modal-root"></div>
  <div id="toast" class="toast"></div>

  <script src="js/normalize.js"></script>
  <script src="js/db.js"></script>
  <script src="js/app.js"></script>
</body>
</html>
```

- [ ] **Step 3: app.js に最小ナビゲーションとトーストを実装**

BtoB の `navigateTo` / `showToast` / `escHtml` / `openModal` / `closeModal` を流用（ページ読み込みフックは後続タスクで足す）。

```js
/* © 2026 Nozomi Sakurada. All rights reserved. */
function navigateTo(pageId){
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
  document.getElementById('page-'+pageId)?.classList.add('active');
  document.querySelector(`.nav-item[data-page="${pageId}"]`)?.classList.add('active');
  if(pageId==='customers') loadCustomers();
  if(pageId==='equipments') loadEquipments();
  if(pageId==='records') loadRecords();
  if(pageId==='intake') loadIntake();
}
function showToast(msg,type='success'){const t=document.getElementById('toast');t.textContent=msg;t.className=`toast toast-${type} show`;setTimeout(()=>t.classList.remove('show'),3000);}
function escHtml(s){return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
function openModal(id){document.getElementById(id)?.classList.add('open');}
function closeModal(id){document.getElementById(id)?.classList.remove('open');}
function today(){return new Date().toISOString().slice(0,10);}
function fmtDate(s){if(!s)return '—';const d=new Date(s);return isNaN(d)?s:`${d.getFullYear()}/${String(d.getMonth()+1).padStart(2,'0')}/${String(d.getDate()).padStart(2,'0')}`;}
function fmtYen(n){if(n===null||n===undefined||n==='')return '¥0';return '¥'+Math.round(Number(n)).toLocaleString('ja-JP');}

// 後続タスクで本実装に差し替えるスタブ
function loadCustomers(){}
function loadEquipments(){}
function loadRecords(){}
function loadIntake(){}
function connectDataFolder(){}
function disconnectDataFolder(){}
function reconnectAllFiles(){}

window.addEventListener('DOMContentLoaded',()=>{
  document.querySelectorAll('.nav-item[data-page]').forEach(el=>{
    el.addEventListener('click',()=>navigateTo(el.dataset.page));
  });
  if(window.lucide) lucide.createIcons();
});
```

- [ ] **Step 4: ブラウザで確認**

`Service Book/index.html` を Chrome で開く。
Expected: ヘッダーとタブが表示され、各タブをクリックすると `.active` が切り替わる（5ページが空で表示される）。コンソールにエラーが出ないこと。

- [ ] **Step 5: コミット**

```bash
git add "Service Book/index.html" "Service Book/css/style.css" "Service Book/js/normalize.js" "Service Book/js/db.js" "Service Book/js/app.js"
git commit -m "feat(service-book): scaffold tabs, css, app shell"
```

---

## Task 1: normalize.js（半角全角の自動正規化＋あいまい検索）

**Files:**
- Modify: `Service Book/js/normalize.js`

- [ ] **Step 1: 正規化・検索関数を実装**

```js
/* © 2026 Nozomi Sakurada. All rights reserved. */
/* Service Book - 半角全角正規化 / あいまい検索ユーティリティ */

/** 全角英数記号→半角（ASCII相当の全角 U+FF01–FF5E と全角スペース U+3000） */
function toHalfWidthAscii(str){
  return String(str||'')
    .replace(/[！-～]/g, ch => String.fromCharCode(ch.charCodeAt(0)-0xFEE0))
    .replace(/　/g,' ');
}

/** 半角カタカナ→全角カタカナ（濁点・半濁点を合成） */
function toFullWidthKana(str){
  const map={'ｶﾞ':'ガ','ｷﾞ':'ギ','ｸﾞ':'グ','ｹﾞ':'ゲ','ｺﾞ':'ゴ','ｻﾞ':'ザ','ｼﾞ':'ジ','ｽﾞ':'ズ','ｾﾞ':'ゼ','ｿﾞ':'ゾ','ﾀﾞ':'ダ','ﾁﾞ':'ヂ','ﾂﾞ':'ヅ','ﾃﾞ':'デ','ﾄﾞ':'ド','ﾊﾞ':'バ','ﾋﾞ':'ビ','ﾌﾞ':'ブ','ﾍﾞ':'ベ','ﾎﾞ':'ボ','ﾊﾟ':'パ','ﾋﾟ':'ピ','ﾌﾟ':'プ','ﾍﾟ':'ペ','ﾎﾟ':'ポ','ｳﾞ':'ヴ'};
  const single={'ｱ':'ア','ｲ':'イ','ｳ':'ウ','ｴ':'エ','ｵ':'オ','ｶ':'カ','ｷ':'キ','ｸ':'ク','ｹ':'ケ','ｺ':'コ','ｻ':'サ','ｼ':'シ','ｽ':'ス','ｾ':'セ','ｿ':'ソ','ﾀ':'タ','ﾁ':'チ','ﾂ':'ツ','ﾃ':'テ','ﾄ':'ト','ﾅ':'ナ','ﾆ':'ニ','ﾇ':'ヌ','ﾈ':'ネ','ﾉ':'ノ','ﾊ':'ハ','ﾋ':'ヒ','ﾌ':'フ','ﾍ':'ヘ','ﾎ':'ホ','ﾏ':'マ','ﾐ':'ミ','ﾑ':'ム','ﾒ':'メ','ﾓ':'モ','ﾔ':'ヤ','ﾕ':'ユ','ﾖ':'ヨ','ﾗ':'ラ','ﾘ':'リ','ﾙ':'ル','ﾚ':'レ','ﾛ':'ロ','ﾜ':'ワ','ｦ':'ヲ','ﾝ':'ン','ｧ':'ァ','ｨ':'ィ','ｩ':'ゥ','ｪ':'ェ','ｫ':'ォ','ｬ':'ャ','ｭ':'ュ','ｮ':'ョ','ｯ':'ッ','ｰ':'ー','｡':'。','｢':'「','｣':'」','､':'、','･':'・'};
  let s=String(str||'');
  Object.keys(map).forEach(k=>{ s=s.split(k).join(map[k]); });
  Object.keys(single).forEach(k=>{ s=s.split(k).join(single[k]); });
  return s;
}

/**
 * フィールド値の保存用正規化（自動変換）
 * - 英数記号は半角へ、半角カナは全角カナへ統一
 * - 前後の空白を除去
 */
function normalizeFieldValue(str){
  return toFullWidthKana(toHalfWidthAscii(str)).trim();
}

/**
 * 検索用キー（表記ゆれ吸収）
 * - 半角化＋全角カナ化のうえ、小文字化・空白/ハイフン除去
 */
function normalizeForSearch(str){
  let s = toFullWidthKana(toHalfWidthAscii(str)).toLowerCase();
  return s.replace(/[\s\-ー‐-―~～]/g,''); // 空白・各種ハイフン・長音・チルダを除去
}

/** あいまい一致: クエリ正規化キーが対象いずれかの正規化キーに部分一致 */
function fuzzyMatch(query, ...targets){
  const q = normalizeForSearch(query);
  if(!q) return true;
  return targets.some(t => normalizeForSearch(t).includes(q));
}

if (typeof window !== 'undefined') {
  window.normalizeFieldValue = normalizeFieldValue;
  window.normalizeForSearch = normalizeForSearch;
  window.fuzzyMatch = fuzzyMatch;
}
```

- [ ] **Step 2: コンソール自己テストで検証**

`index.html` を開いた状態で DevTools コンソールに貼り付けて実行：

```js
console.assert(normalizeFieldValue('ＧＨＨ２４００')==='GHH2400','half-width');
console.assert(normalizeFieldValue('ｶﾞｽ')==='ガス','kana');
console.assert(fuzzyMatch('ghh-2400','ＧＨＨ 2400')===true,'fuzzy hit');
console.assert(fuzzyMatch('リンナイ','ﾘﾝﾅｲ給湯器')===true,'fuzzy kana');
console.assert(fuzzyMatch('xyz','GHH2400')===false,'fuzzy miss');
console.log('normalize.js OK');
```

Expected: コンソールに `normalize.js OK` のみ表示（`Assertion failed` が一切出ない）。

- [ ] **Step 3: コミット**

```bash
git add "Service Book/js/normalize.js"
git commit -m "feat(service-book): add half/full-width normalization and fuzzy search util"
```

---

## Task 2: db.js（IndexedDB ストア＋汎用CRUD）

**Files:**
- Modify: `Service Book/js/db.js`

- [ ] **Step 1: DB定義とCRUDを実装**

[../BtoB Price data base/js/db.js](../BtoB%20Price%20data%20base/js/db.js) の汎用CRUD（`dbAdd/dbPut/dbGet/dbGetAll/dbGetByIndex/dbDelete/dbDeleteByIndex/dbExportPartial/dbImportPartial`）をそのままコピーし、`openDB` のストア定義のみ以下に差し替える。

```js
/* © 2026 Nozomi Sakurada. All rights reserved. */
/* Service Book - IndexedDB管理モジュール */
const DB_NAME='ServiceBookDB';
const DB_VERSION=1;
let _db=null;

function openDB(){
  return new Promise((resolve,reject)=>{
    if(_db){resolve(_db);return;}
    const req=indexedDB.open(DB_NAME,DB_VERSION);
    req.onupgradeneeded=(e)=>{
      const db=e.target.result;
      if(!db.objectStoreNames.contains('customers')){
        db.createObjectStore('customers',{keyPath:'id',autoIncrement:true});
      }
      if(!db.objectStoreNames.contains('equipments')){
        const es=db.createObjectStore('equipments',{keyPath:'id',autoIncrement:true});
        es.createIndex('customerId','customerId',{unique:false});
      }
      if(!db.objectStoreNames.contains('serviceRecords')){
        const rs=db.createObjectStore('serviceRecords',{keyPath:'id',autoIncrement:true});
        rs.createIndex('equipmentId','equipmentId',{unique:false});
        rs.createIndex('status','status',{unique:false});
      }
      if(!db.objectStoreNames.contains('appSettings')){
        db.createObjectStore('appSettings');
      }
    };
    req.onsuccess=(e)=>{_db=e.target.result;resolve(_db);};
    req.onerror=(e)=>reject(e.target.error);
  });
}
```

そのままコピーする汎用CRUD（BtoB db.js の 67–186 行と同一・ストア名は引数で渡すため変更不要）：`dbAdd`, `dbPut`, `dbGet`, `dbGetAll`, `dbGetByIndex`, `dbDelete`, `dbDeleteByIndex`, `dbExportPartial`, `dbImportPartial`。

- [ ] **Step 2: コンソールで疎通確認**

`index.html` を開き直し、コンソールで：

```js
await openDB();
const id = await dbAdd('customers',{name:'テスト瓦斯',kana:'テストガス'});
console.log('added id', id, await dbGet('customers', id));
await dbDelete('customers', id);
console.log('count', (await dbGetAll('customers')).length);
```

Expected: `added id 1 {...name:'テスト瓦斯'...}` が出て、最後の `count` が `0`。エラーなし。

- [ ] **Step 3: コミット**

```bash
git add "Service Book/js/db.js"
git commit -m "feat(service-book): IndexedDB stores (customers/equipments/serviceRecords) + CRUD"
```

---

## Task 3: フォルダ同期（Directory Access API）

**Files:**
- Modify: `Service Book/js/app.js`

- [ ] **Step 1: 同期設定とロジックを移植**

[../BtoB Price data base/js/app.js](../BtoB%20Price%20data%20base/js/app.js) の 1026–1384 行（`SYNC_CONFIG`〜`reconnectAllFiles`）を `app.js` にコピーし、`SYNC_CONFIG` と `_syncHandles` のみ差し替える。`onXxxChanged` トリガも置き換える。

```js
const SYNC_CONFIG = {
  customers: { filename:'customers.json',        stores:['customers'] },
  equipments:{ filename:'equipments.json',       stores:['equipments'] },
  records:   { filename:'service_records.json',  stores:['serviceRecords'] }
};
let _syncHandles = { customers:null, equipments:null, records:null };
let _dirHandle = null;

async function onCustomersChanged(){ await saveToLocalFile('customers'); }
async function onEquipmentsChanged(){ await saveToLocalFile('equipments'); }
async function onRecordsChanged(){ await saveToLocalFile('records'); }
```

`loadFromLocalFile` 内の「表示中ページ更新」分岐は本ツールのページIDへ修正：

```js
if(dbKey==='customers' && document.getElementById('page-customers').classList.contains('active')) loadCustomers();
if(dbKey==='equipments' && document.getElementById('page-equipments').classList.contains('active')) loadEquipments();
if(dbKey==='records' && document.getElementById('page-records').classList.contains('active')) loadRecords();
```

`updateFolderStatus` のうち、本ツールに無いDOM（`sync-dot-*` 等）への参照は `if(!dot||!label) return;` のガードで無害化されるため、そのまま流用可。Task0 のスタブ `connectDataFolder/disconnectDataFolder/reconnectAllFiles` は削除する（移植版が本実装）。

- [ ] **Step 2: 起動時フックを追加**

`DOMContentLoaded` ハンドラ（Task0 で作成済み）の先頭に同期初期化を追加：

```js
await openDB();
await checkAutoSync();
updateFolderStatus();
navigateTo('intake');
```

- [ ] **Step 3: ブラウザで確認**

`index.html` を開く → ヘッダーに「dataフォルダ未接続」バナー表示。「フォルダを接続」→ `Service Book/data` を選択。
Expected: 接続済みバナーに切替わり、`Service Book/data/` に `customers.json` / `equipments.json` / `service_records.json` が生成される（中身は `{"customers":[]}` 等）。`showToast` が表示される。

- [ ] **Step 4: コミット**

```bash
git add "Service Book/js/app.js"
git commit -m "feat(service-book): data folder sync via Directory Access API"
```

---

## Task 4: 顧客管理タブ（CRUD＋正規化＋あいまい検索）

**Files:**
- Modify: `Service Book/js/app.js`
- Modify: `Service Book/index.html`（顧客ページUI・モーダル）

- [ ] **Step 1: 顧客ページUIをindex.htmlに追加**

`#page-customers` の中身：

```html
<div class="page-head">
  <h2>顧客管理</h2>
  <div class="page-actions">
    <input id="customer-search" class="search" type="search" placeholder="顧客名・住所・連絡先で検索">
    <button class="btn btn-primary" type="button" onclick="openCustomerModal()">顧客を追加</button>
  </div>
</div>
<table class="data-table">
  <thead><tr><th>顧客名</th><th>連絡先</th><th>住所</th><th>ハウスメーカー</th><th>操作</th></tr></thead>
  <tbody id="customer-list"></tbody>
</table>
```

`#modal-root` に顧客モーダルを追加：

```html
<div id="customer-modal" class="modal">
  <div class="modal-box">
    <h3 id="customer-modal-title">顧客を追加</h3>
    <input type="hidden" id="customer-id">
    <label>顧客名<input id="customer-name" type="text"></label>
    <label>フリガナ<input id="customer-kana" type="text"></label>
    <label>連絡先（担当・TEL）<input id="customer-contact" type="text"></label>
    <label>住所<input id="customer-address" type="text"></label>
    <label>依頼元 / 販売店<input id="customer-dealerName" type="text"></label>
    <label>ハウスメーカー<input id="customer-houseMaker" type="text"></label>
    <label>特記事項（報告書に印刷）<textarea id="customer-printNote"></textarea></label>
    <label>備考（社内用・印刷されません）<textarea id="customer-memo"></textarea></label>
    <div class="modal-actions">
      <button class="btn btn-secondary" type="button" onclick="closeModal('customer-modal')">キャンセル</button>
      <button class="btn btn-success" type="button" onclick="saveCustomer()">保存</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: 一覧・あいまい検索を実装**

```js
async function loadCustomers(filterText=''){
  const all = await dbGetAll('customers');
  const filtered = filterText
    ? all.filter(c => fuzzyMatch(filterText, c.name, c.address, c.contact, c.kana))
    : all;
  const tbody = document.getElementById('customer-list');
  tbody.innerHTML = filtered.length===0
    ? `<tr><td colspan="5" class="text-center" style="padding:32px;color:var(--text3)">顧客が登録されていません</td></tr>`
    : filtered.map(c=>`<tr>
        <td><strong style="cursor:pointer;color:var(--info);text-decoration:underline" onclick="goEquipmentsOf(${c.id})">${escHtml(c.name)}</strong>
            <div style="font-size:11px;color:var(--text3)">${escHtml(c.kana||'')}</div></td>
        <td>${escHtml(c.contact||'')}</td>
        <td>${escHtml(c.address||'')}</td>
        <td>${escHtml(c.houseMaker||'')}</td>
        <td><div style="display:flex;gap:6px">
          <button class="btn btn-xs btn-secondary" type="button" onclick="openCustomerModal(${c.id})">編集</button>
          <button class="btn btn-xs btn-danger" type="button" onclick="deleteCustomer(${c.id})">削除</button>
        </div></td>
      </tr>`).join('');
}
function goEquipmentsOf(customerId){ _equipFilterCustomerId = customerId; navigateTo('equipments'); }
```

- [ ] **Step 3: モーダル開閉・保存（正規化適用）・削除を実装**

```js
const CUSTOMER_FIELDS = ['name','kana','contact','address','dealerName','houseMaker','printNote','memo'];

async function openCustomerModal(id=null){
  let data={};
  if(id){ data = await dbGet('customers', id) || {}; }
  document.getElementById('customer-modal-title').textContent = id?'顧客を編集':'顧客を追加';
  document.getElementById('customer-id').value = id || '';
  CUSTOMER_FIELDS.forEach(f=>{ document.getElementById('customer-'+f).value = data[f] || ''; });
  openModal('customer-modal');
}

async function saveCustomer(){
  const id = document.getElementById('customer-id').value;
  const name = normalizeFieldValue(document.getElementById('customer-name').value);
  if(!name){ alert('顧客名を入力してください'); return; }
  const data = {};
  CUSTOMER_FIELDS.forEach(f=>{ data[f] = normalizeFieldValue(document.getElementById('customer-'+f).value); });
  if(id){ data.id = parseInt(id); await dbPut('customers', data); }
  else { await dbAdd('customers', data); }
  closeModal('customer-modal');
  await onCustomersChanged();
  showToast('顧客情報を保存しました');
  loadCustomers();
}

async function deleteCustomer(id){
  if(!window.confirm('この顧客を削除しますか？（関連する機器・記録は残りますが紐付けが切れます）')) return;
  await dbDelete('customers', id);
  await onCustomersChanged();
  showToast('削除しました','danger');
  loadCustomers();
}
```

- [ ] **Step 4: 検索ボックスのイベントを登録**

`DOMContentLoaded` ハンドラ末尾に追加：

```js
document.getElementById('customer-search').addEventListener('input', e=>loadCustomers(e.target.value));
```

- [ ] **Step 5: ブラウザで確認**

顧客管理タブ →「顧客を追加」→ 顧客名に全角「ＡＢＣ商事」、フリガナに半角ｶﾅで「ｴｰﾋﾞｰｼｰ」を入力して保存。
Expected: 一覧に「ABC商事」（半角化）・フリガナ「エービーシー」（全角カナ化）で表示。検索欄に `abc` と入れると当該行が残る。`data/customers.json` に1件書き込まれる。編集で値が復元、削除で消える。

- [ ] **Step 6: コミット**

```bash
git add "Service Book/index.html" "Service Book/js/app.js"
git commit -m "feat(service-book): customer CRUD with normalization and fuzzy search"
```

---

## Task 5: 機器管理タブ（顧客紐付け・CRUD）

**Files:**
- Modify: `Service Book/js/app.js`
- Modify: `Service Book/index.html`（機器ページUI・モーダル）

- [ ] **Step 1: 機器ページUIをindex.htmlに追加**

```html
<div class="page-head">
  <h2>機器管理</h2>
  <div class="page-actions">
    <select id="equipment-customer-filter"></select>
    <input id="equipment-search" class="search" type="search" placeholder="型式・メーカー・用途で検索">
    <button class="btn btn-primary" type="button" onclick="openEquipmentModal()">機器を追加</button>
  </div>
</div>
<table class="data-table">
  <thead><tr><th>顧客</th><th>メーカー / 型式</th><th>製品ロット</th><th>用途</th><th>設置日</th><th>操作</th></tr></thead>
  <tbody id="equipment-list"></tbody>
</table>
```

機器モーダルを `#modal-root` に追加：

```html
<div id="equipment-modal" class="modal">
  <div class="modal-box">
    <h3 id="equipment-modal-title">機器を追加</h3>
    <input type="hidden" id="equipment-id">
    <label>顧客<select id="equipment-customerId"></select></label>
    <label>メーカー<input id="equipment-maker" type="text" placeholder="パーパス / ノーリツ / リンナイ / パロマ"></label>
    <label>型式<input id="equipment-modelNo" type="text"></label>
    <label>製品ロット / 製造番号<input id="equipment-lotNo" type="text"></label>
    <label>機器区分<input id="equipment-category" type="text" placeholder="給湯器 / 風呂釜 / レンジフード"></label>
    <label>ガス種別<input id="equipment-gasType" type="text" placeholder="都市ガス / LPG"></label>
    <label>設置日<input id="equipment-installDate" type="date"></label>
    <label>用途<input id="equipment-usage" type="text" placeholder="浴室給湯 / 厨房給湯 / 暖房"></label>
    <label>備考<textarea id="equipment-memo"></textarea></label>
    <div class="modal-actions">
      <button class="btn btn-secondary" type="button" onclick="closeModal('equipment-modal')">キャンセル</button>
      <button class="btn btn-success" type="button" onclick="saveEquipment()">保存</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: 一覧・顧客フィルタ・あいまい検索を実装**

```js
let _equipFilterCustomerId = null; // 顧客タブからの遷移で設定

async function loadEquipments(filterText=''){
  const [equips, customers] = await Promise.all([dbGetAll('equipments'), dbGetAll('customers')]);
  const cmap = Object.fromEntries(customers.map(c=>[c.id,c]));

  // 顧客フィルタ <select> を構築
  const fsel = document.getElementById('equipment-customer-filter');
  fsel.innerHTML = `<option value="">全顧客</option>` + customers.map(c=>
    `<option value="${c.id}" ${_equipFilterCustomerId==c.id?'selected':''}>${escHtml(c.name)}</option>`).join('');

  let list = equips;
  if(_equipFilterCustomerId) list = list.filter(e=>e.customerId==_equipFilterCustomerId);
  if(filterText) list = list.filter(e=>fuzzyMatch(filterText, e.modelNo, e.maker, e.usage, e.lotNo));

  const tbody = document.getElementById('equipment-list');
  tbody.innerHTML = list.length===0
    ? `<tr><td colspan="6" class="text-center" style="padding:32px;color:var(--text3)">機器が登録されていません</td></tr>`
    : list.map(e=>`<tr>
        <td>${cmap[e.customerId]?escHtml(cmap[e.customerId].name):'—'}</td>
        <td>${escHtml(e.maker||'')}<br><strong>${escHtml(e.modelNo||'')}</strong></td>
        <td>${escHtml(e.lotNo||'')}</td>
        <td>${escHtml(e.usage||'')}</td>
        <td>${fmtDate(e.installDate)}</td>
        <td><div style="display:flex;gap:6px">
          <button class="btn btn-xs btn-secondary" type="button" onclick="goRecordsOf(${e.id})">記録</button>
          <button class="btn btn-xs btn-secondary" type="button" onclick="openEquipmentModal(${e.id})">編集</button>
          <button class="btn btn-xs btn-danger" type="button" onclick="deleteEquipment(${e.id})">削除</button>
        </div></td>
      </tr>`).join('');
}
function goRecordsOf(equipmentId){ _recordFilterEquipmentId = equipmentId; navigateTo('records'); }
```

- [ ] **Step 3: モーダル・保存（正規化）・削除を実装**

```js
const EQUIP_TEXT_FIELDS = ['maker','modelNo','lotNo','category','gasType','usage','memo'];

async function openEquipmentModal(id=null){
  const customers = await dbGetAll('customers');
  let data = {};
  if(id){ data = await dbGet('equipments', id) || {}; }
  document.getElementById('equipment-modal-title').textContent = id?'機器を編集':'機器を追加';
  document.getElementById('equipment-id').value = id || '';
  const sel = document.getElementById('equipment-customerId');
  sel.innerHTML = `<option value="">— 顧客を選択 —</option>` + customers.map(c=>
    `<option value="${c.id}" ${data.customerId==c.id?'selected':''}>${escHtml(c.name)}</option>`).join('');
  // 機器タブで顧客フィルタ中なら初期選択
  if(!id && _equipFilterCustomerId) sel.value = _equipFilterCustomerId;
  EQUIP_TEXT_FIELDS.forEach(f=>{ document.getElementById('equipment-'+f).value = data[f]||''; });
  document.getElementById('equipment-installDate').value = data.installDate || '';
  openModal('equipment-modal');
}

async function saveEquipment(){
  const id = document.getElementById('equipment-id').value;
  const customerId = parseInt(document.getElementById('equipment-customerId').value) || null;
  if(!customerId){ alert('顧客を選択してください'); return; }
  const modelNo = normalizeFieldValue(document.getElementById('equipment-modelNo').value);
  if(!modelNo){ alert('型式を入力してください'); return; }
  const data = { customerId, installDate: document.getElementById('equipment-installDate').value };
  EQUIP_TEXT_FIELDS.forEach(f=>{ data[f] = normalizeFieldValue(document.getElementById('equipment-'+f).value); });
  if(id){ data.id = parseInt(id); await dbPut('equipments', data); }
  else { await dbAdd('equipments', data); }
  closeModal('equipment-modal');
  await onEquipmentsChanged();
  showToast('機器情報を保存しました');
  loadEquipments();
}

async function deleteEquipment(id){
  if(!window.confirm('この機器を削除しますか？')) return;
  await dbDelete('equipments', id);
  await onEquipmentsChanged();
  showToast('削除しました','danger');
  loadEquipments();
}
```

- [ ] **Step 4: フィルタ/検索イベントを登録**

`DOMContentLoaded` 末尾に追加：

```js
document.getElementById('equipment-search').addEventListener('input', e=>loadEquipments(e.target.value));
document.getElementById('equipment-customer-filter').addEventListener('change', e=>{ _equipFilterCustomerId = e.target.value?parseInt(e.target.value):null; loadEquipments(); });
```

- [ ] **Step 5: ブラウザで確認**

顧客を1件用意 → 機器管理 →「機器を追加」→ 顧客選択・メーカー「ﾘﾝﾅｲ」・型式「ｇｈｈ２４００」・用途「浴室給湯」を保存。
Expected: 一覧にメーカー「リンナイ」・型式「GHH2400」で表示。検索 `ghh-2400` で当該行が残る。顧客タブで顧客名クリック→機器タブにフィルタ適用されて遷移。`data/equipments.json` に書き込まれる。

- [ ] **Step 6: コミット**

```bash
git add "Service Book/index.html" "Service Book/js/app.js"
git commit -m "feat(service-book): equipment CRUD linked to customer"
```

---

## Task 6: サービス記録タブ（記録CRUD・完了化・履歴・放置検知）

**Files:**
- Modify: `Service Book/js/app.js`
- Modify: `Service Book/index.html`（記録ページUI・モーダル）

- [ ] **Step 1: 放置検知ヘルパーを実装**

```js
const STALE_DAYS = 14; // 2週間。閾値はここで調整。

/** 未完了かつ最終更新から STALE_DAYS 以上経過なら経過日数を返す。対象外は null */
function staleDaysOf(rec){
  if(rec.status==='completed') return null;
  const base = rec.updatedAt || rec.createdAt;
  if(!base) return null;
  const days = Math.floor((Date.now() - new Date(base).getTime())/(1000*60*60*24));
  return days >= STALE_DAYS ? days : null;
}
```

- [ ] **Step 2: 記録ページUIをindex.htmlに追加**

```html
<div class="page-head">
  <h2>サービス記録</h2>
  <div class="page-actions">
    <select id="record-equipment-filter"></select>
    <span id="stale-summary" class="badge badge-expired" style="display:none"></span>
    <button class="btn btn-primary" type="button" onclick="openRecordModal()">記録を追加</button>
  </div>
</div>
<table class="data-table">
  <thead><tr><th>状態</th><th>受付日 / 対応日</th><th>管理番号</th><th>機器</th><th>作業種別</th><th>担当</th><th>操作</th></tr></thead>
  <tbody id="record-list"></tbody>
</table>
```

記録モーダルを `#modal-root` に追加（依頼発行タブと共用）：

```html
<div id="record-modal" class="modal">
  <div class="modal-box modal-wide">
    <h3 id="record-modal-title">記録を追加</h3>
    <input type="hidden" id="record-id">
    <label>機器<select id="record-equipmentId"></select></label>
    <div class="grid2">
      <label>管理番号<input id="record-mgmtNo" type="text"></label>
      <label>作業種別<select id="record-workType">
        <option>定期点検</option><option>故障修理</option><option>アラーム対応</option><option>設置</option>
      </select></label>
      <label>受付日<input id="record-receivedDate" type="date"></label>
      <label>受付者<input id="record-receptionist" type="text"></label>
      <label>希望日<input id="record-requestedDate" type="date"></label>
      <label>対応日<input id="record-serviceDate" type="date"></label>
      <label>完了日<input id="record-completionDate" type="date"></label>
      <label>担当者<input id="record-staff" type="text"></label>
    </div>
    <label>コール内容<textarea id="record-callContent"></textarea></label>
    <label>担当者所見<textarea id="record-remarks"></textarea></label>
    <label>お客様への報告<textarea id="record-customerReport"></textarea></label>
    <h4>交換部品</h4>
    <table class="data-table"><thead><tr><th>品名</th><th>数量</th><th>単価</th><th></th></tr></thead>
      <tbody id="record-parts"></tbody></table>
    <button class="btn btn-xs btn-secondary" type="button" onclick="addPartRow()">部品を追加</button>
    <div class="grid2" style="margin-top:12px">
      <label>出張料<input id="record-visitFee" type="number"></label>
      <label>技術料<input id="record-laborFee" type="number"></label>
      <div>部品代: <span id="record-partsFee">¥0</span></div>
      <div>合計: <strong id="record-total">¥0</strong></div>
    </div>
    <div class="modal-actions">
      <button class="btn btn-secondary" type="button" onclick="closeModal('record-modal')">キャンセル</button>
      <button class="btn btn-success" type="button" onclick="saveRecord()">保存</button>
    </div>
  </div>
</div>
```

- [ ] **Step 3: 一覧・機器フィルタ・放置強調を実装**

```js
let _recordFilterEquipmentId = null;

async function loadRecords(){
  const [recs, equips, customers] = await Promise.all([
    dbGetAll('serviceRecords'), dbGetAll('equipments'), dbGetAll('customers')]);
  const emap = Object.fromEntries(equips.map(e=>[e.id,e]));
  const cmap = Object.fromEntries(customers.map(c=>[c.id,c]));

  // 機器フィルタ
  const fsel = document.getElementById('record-equipment-filter');
  fsel.innerHTML = `<option value="">全機器</option>` + equips.map(e=>{
    const c = cmap[e.customerId];
    return `<option value="${e.id}" ${_recordFilterEquipmentId==e.id?'selected':''}>${escHtml((c?c.name+' / ':'')+(e.modelNo||''))}</option>`;
  }).join('');

  let list = [...recs];
  if(_recordFilterEquipmentId) list = list.filter(r=>r.equipmentId==_recordFilterEquipmentId);
  // 時系列（新しい順）
  list.sort((a,b)=>(b.receivedDate||b.createdAt||'')>(a.receivedDate||a.createdAt||'')?1:-1);

  // 放置件数バッジ（全体）
  const staleCount = recs.filter(r=>staleDaysOf(r)!==null).length;
  const navBadge = document.getElementById('nav-badge-stale');
  navBadge.textContent = staleCount; navBadge.style.display = staleCount>0?'':'none';
  const sum = document.getElementById('stale-summary');
  sum.textContent = `放置案件 ${staleCount}件`; sum.style.display = staleCount>0?'':'none';

  const tbody = document.getElementById('record-list');
  tbody.innerHTML = list.length===0
    ? `<tr><td colspan="7" class="text-center" style="padding:32px;color:var(--text3)">記録がありません</td></tr>`
    : list.map(r=>{
        const e = emap[r.equipmentId];
        const c = e?cmap[e.customerId]:null;
        const stale = staleDaysOf(r);
        const rowStyle = stale!==null ? 'background:#fff1f0' : '';
        const statusBadge = r.status==='completed'
          ? `<span class="badge badge-active">完了</span>`
          : stale!==null
            ? `<span class="badge badge-expired">放置 ${stale}日</span>`
            : `<span class="badge badge-warning">進行中</span>`;
        return `<tr style="${rowStyle}">
          <td>${statusBadge}</td>
          <td>${fmtDate(r.receivedDate)}<br><span style="font-size:11px;color:var(--text3)">${fmtDate(r.serviceDate)}</span></td>
          <td>${escHtml(r.mgmtNo||'')}</td>
          <td>${c?escHtml(c.name):'—'}<br><span style="font-size:11px;color:var(--text3)">${e?escHtml(e.modelNo||''):''}</span></td>
          <td>${escHtml(r.workType||'')}</td>
          <td>${escHtml(r.staff||'')}</td>
          <td><div style="display:flex;gap:6px;flex-wrap:wrap">
            <button class="btn btn-xs btn-secondary" type="button" onclick="openRecordModal(${r.id})">編集</button>
            <button class="btn btn-xs btn-primary" type="button" onclick="openPrint(${r.id})">報告書</button>
            <button class="btn btn-xs btn-danger" type="button" onclick="deleteRecord(${r.id})">削除</button>
          </div></td>
        </tr>`;
      }).join('');
}
```

- [ ] **Step 4: 部品行・料金合計のUIロジックを実装**

```js
let _partRows = []; // [{name, qty, unitPrice}]

function renderParts(){
  const tbody = document.getElementById('record-parts');
  tbody.innerHTML = _partRows.map((p,i)=>`<tr>
    <td><input type="text" value="${escHtml(p.name||'')}" oninput="updatePart(${i},'name',this.value)"></td>
    <td><input type="number" value="${p.qty??''}" style="width:70px" oninput="updatePart(${i},'qty',this.value)"></td>
    <td><input type="number" value="${p.unitPrice??''}" style="width:100px" oninput="updatePart(${i},'unitPrice',this.value)"></td>
    <td><button class="btn btn-xs btn-danger" type="button" onclick="removePart(${i})">×</button></td>
  </tr>`).join('');
  recalcFee();
}
function addPartRow(){ _partRows.push({name:'',qty:1,unitPrice:0}); renderParts(); }
function updatePart(i,f,v){ _partRows[i][f] = (f==='name')?v:(parseFloat(v)||0); recalcFee(); }
function removePart(i){ _partRows.splice(i,1); renderParts(); }
function recalcFee(){
  const partsFee = _partRows.reduce((s,p)=>s+(Number(p.qty)||0)*(Number(p.unitPrice)||0),0);
  const visit = parseFloat(document.getElementById('record-visitFee').value)||0;
  const labor = parseFloat(document.getElementById('record-laborFee').value)||0;
  document.getElementById('record-partsFee').textContent = fmtYen(partsFee);
  document.getElementById('record-total').textContent = fmtYen(visit+labor+partsFee);
}
```

- [ ] **Step 5: モーダル開閉・保存（status/timestamps/正規化）・削除を実装**

```js
async function openRecordModal(id=null){
  const equips = await dbGetAll('equipments');
  const customers = await dbGetAll('customers');
  const cmap = Object.fromEntries(customers.map(c=>[c.id,c]));
  let data = {};
  if(id){ data = await dbGet('serviceRecords', id) || {}; }
  document.getElementById('record-modal-title').textContent = id?'記録を編集':'記録を追加';
  document.getElementById('record-id').value = id || '';
  const sel = document.getElementById('record-equipmentId');
  sel.innerHTML = `<option value="">— 機器を選択 —</option>` + equips.map(e=>{
    const c=cmap[e.customerId]; return `<option value="${e.id}" ${data.equipmentId==e.id?'selected':''}>${escHtml((c?c.name+' / ':'')+(e.modelNo||''))}</option>`;
  }).join('');
  if(!id && _recordFilterEquipmentId) sel.value = _recordFilterEquipmentId;
  ['mgmtNo','receptionist','staff','callContent','remarks','customerReport'].forEach(f=>{
    document.getElementById('record-'+f).value = data[f]||''; });
  ['receivedDate','requestedDate','serviceDate','completionDate'].forEach(f=>{
    document.getElementById('record-'+f).value = data[f]||''; });
  document.getElementById('record-workType').value = data.workType || '定期点検';
  document.getElementById('record-visitFee').value = data.fee?.visitFee ?? '';
  document.getElementById('record-laborFee').value = data.fee?.laborFee ?? '';
  if(!data.mgmtNo && !id) document.getElementById('record-mgmtNo').value = await nextMgmtNo();
  _partRows = Array.isArray(data.parts) ? data.parts.map(p=>({...p})) : [];
  renderParts();
  openModal('record-modal');
}

async function saveRecord(){
  const id = document.getElementById('record-id').value;
  const equipmentId = parseInt(document.getElementById('record-equipmentId').value)||null;
  if(!equipmentId){ alert('機器を選択してください'); return; }
  const completionDate = document.getElementById('record-completionDate').value;
  const parts = _partRows.filter(p=>p.name||p.qty||p.unitPrice)
    .map(p=>({name:normalizeFieldValue(p.name), qty:Number(p.qty)||0, unitPrice:Number(p.unitPrice)||0}));
  const partsFee = parts.reduce((s,p)=>s+p.qty*p.unitPrice,0);
  const visitFee = parseFloat(document.getElementById('record-visitFee').value)||0;
  const laborFee = parseFloat(document.getElementById('record-laborFee').value)||0;
  const now = today();
  const base = id ? (await dbGet('serviceRecords', parseInt(id))) || {} : {};
  const data = {
    ...base,
    equipmentId,
    mgmtNo: normalizeFieldValue(document.getElementById('record-mgmtNo').value),
    receivedDate: document.getElementById('record-receivedDate').value,
    receptionist: normalizeFieldValue(document.getElementById('record-receptionist').value),
    requestedDate: document.getElementById('record-requestedDate').value,
    serviceDate: document.getElementById('record-serviceDate').value,
    completionDate,
    staff: normalizeFieldValue(document.getElementById('record-staff').value),
    workType: document.getElementById('record-workType').value,
    callContent: document.getElementById('record-callContent').value.trim(),
    remarks: document.getElementById('record-remarks').value.trim(),
    customerReport: document.getElementById('record-customerReport').value.trim(),
    parts,
    fee: { visitFee, laborFee, partsFee, total: visitFee+laborFee+partsFee },
    status: completionDate ? 'completed' : 'open',
    createdAt: base.createdAt || now,
    updatedAt: now
  };
  if(id){ data.id = parseInt(id); await dbPut('serviceRecords', data); }
  else { await dbAdd('serviceRecords', data); }
  closeModal('record-modal');
  await onRecordsChanged();
  showToast('記録を保存しました');
  loadRecords();
}

async function deleteRecord(id){
  if(!window.confirm('この記録を削除しますか？')) return;
  await dbDelete('serviceRecords', id);
  await onRecordsChanged();
  showToast('削除しました','danger');
  loadRecords();
}
```

- [ ] **Step 6: 管理番号の自動採番を実装**

```js
/** SB-YYYYMMDD-NN 形式。同日連番。 */
async function nextMgmtNo(){
  const recs = await dbGetAll('serviceRecords');
  const prefix = 'SB-' + today().replace(/-/g,'') + '-';
  const todays = recs.filter(r=>(r.mgmtNo||'').startsWith(prefix));
  const seq = String(todays.length+1).padStart(2,'0');
  return prefix + seq;
}
```

- [ ] **Step 7: 料金欄の入力イベントとフィルタを登録**

`DOMContentLoaded` 末尾に追加：

```js
document.getElementById('record-visitFee').addEventListener('input', recalcFee);
document.getElementById('record-laborFee').addEventListener('input', recalcFee);
document.getElementById('record-equipment-filter').addEventListener('change', e=>{ _recordFilterEquipmentId = e.target.value?parseInt(e.target.value):null; loadRecords(); });
```

- [ ] **Step 8: ブラウザで確認**

機器を1件用意 → サービス記録 →「記録を追加」→ 管理番号が自動入りすることを確認。部品「センサー × 2 × 1500」を追加し、出張料3000・技術料5000を入力。
Expected: 部品代 `¥3,000`、合計 `¥11,000` が自動計算。完了日を空のまま保存→一覧に「進行中」。完了日を入れて保存→「完了」。`receivedDate` を15日前にして完了日を空にすると「放置 15日」赤バッジ＋行が赤背景＋ナビに件数バッジ。`data/service_records.json` に書き込み。

- [ ] **Step 9: コミット**

```bash
git add "Service Book/index.html" "Service Book/js/app.js"
git commit -m "feat(service-book): service record CRUD, fees, mgmt-no, stale detection"
```

---

## Task 7: 新規依頼発行タブ（受付→未完了記録生成→依頼票印刷）

**Files:**
- Modify: `Service Book/js/app.js`
- Modify: `Service Book/index.html`（依頼発行ページUI）

- [ ] **Step 1: 依頼発行ページUIをindex.htmlに追加**

依頼発行は「記録モーダルを open 状態前提で開く」入口とする。専用フォームを置き、保存後に依頼票印刷へ誘導する。

```html
<div class="page-head"><h2>新規依頼発行</h2></div>
<div class="intake-card">
  <p>受付情報を入力して依頼（未完了のサービス記録）を起こします。保存後に依頼票を印刷できます。</p>
  <div style="display:flex;gap:8px;flex-wrap:wrap">
    <button class="btn btn-primary btn-lg" type="button" onclick="openIntake()">新規依頼を起こす</button>
    <button class="btn btn-secondary btn-lg" type="button" onclick="openCopyPicker()">過去履歴からコピーして発行</button>
  </div>
  <div style="margin-top:16px">
    <h3>未完了の依頼</h3>
    <table class="data-table">
      <thead><tr><th>管理番号</th><th>受付日</th><th>顧客 / 機器</th><th>作業種別</th><th>操作</th></tr></thead>
      <tbody id="intake-open-list"></tbody>
    </table>
  </div>
</div>
```

過去記録ピッカーのモーダルを `#modal-root` に追加：

```html
<div id="copy-picker-modal" class="modal">
  <div class="modal-box modal-wide">
    <h3>過去履歴からコピー</h3>
    <input id="copy-picker-search" class="search" type="search" placeholder="顧客名・型式・管理番号で検索">
    <table class="data-table">
      <thead><tr><th>受付日</th><th>管理番号</th><th>顧客 / 機器</th><th>作業種別</th><th></th></tr></thead>
      <tbody id="copy-picker-list"></tbody>
    </table>
    <div class="modal-actions">
      <button class="btn btn-secondary" type="button" onclick="closeModal('copy-picker-modal')">閉じる</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: 依頼発行ロジックを実装**

依頼発行は記録モーダルを再利用しつつ「受付日を今日で初期化」して開く。`loadIntake` は未完了一覧と依頼票印刷ボタンを描画。

```js
async function loadIntake(){
  const [recs, equips, customers] = await Promise.all([
    dbGetAll('serviceRecords'), dbGetAll('equipments'), dbGetAll('customers')]);
  const emap = Object.fromEntries(equips.map(e=>[e.id,e]));
  const cmap = Object.fromEntries(customers.map(c=>[c.id,c]));
  const open = recs.filter(r=>r.status!=='completed')
    .sort((a,b)=>(b.receivedDate||'')>(a.receivedDate||'')?1:-1);
  const tbody = document.getElementById('intake-open-list');
  tbody.innerHTML = open.length===0
    ? `<tr><td colspan="5" class="text-center" style="padding:24px;color:var(--text3)">未完了の依頼はありません</td></tr>`
    : open.map(r=>{
        const e=emap[r.equipmentId]; const c=e?cmap[e.customerId]:null;
        return `<tr>
          <td>${escHtml(r.mgmtNo||'')}</td>
          <td>${fmtDate(r.receivedDate)}</td>
          <td>${c?escHtml(c.name):'—'} / ${e?escHtml(e.modelNo||''):'—'}</td>
          <td>${escHtml(r.workType||'')}</td>
          <td><div style="display:flex;gap:6px">
            <button class="btn btn-xs btn-secondary" type="button" onclick="openRecordModal(${r.id})">編集</button>
            <button class="btn btn-xs btn-primary" type="button" onclick="openRequestSlip(${r.id})">依頼票</button>
          </div></td>
        </tr>`;
      }).join('');
}

async function openIntake(){
  await openRecordModal();              // 新規記録モーダル（管理番号自動採番）
  document.getElementById('record-receivedDate').value = today();
  document.getElementById('record-modal-title').textContent = '新規依頼発行';
}
```

- [ ] **Step 3: 保存後に依頼一覧を更新する**

`saveRecord` 末尾の `loadRecords();` の直後に、依頼タブが表示中なら更新を足す：

```js
if(document.getElementById('page-intake').classList.contains('active')) loadIntake();
```

- [ ] **Step 4: 過去履歴ピッカーを実装**

全記録（完了・未完了問わず）を新しい順に一覧し、あいまい検索で絞り込む。各行に「この内容でコピー発行」ボタンを置く。

```js
async function openCopyPicker(){
  document.getElementById('copy-picker-search').value = '';
  await renderCopyPicker('');
  openModal('copy-picker-modal');
}

async function renderCopyPicker(filterText){
  const [recs, equips, customers] = await Promise.all([
    dbGetAll('serviceRecords'), dbGetAll('equipments'), dbGetAll('customers')]);
  const emap = Object.fromEntries(equips.map(e=>[e.id,e]));
  const cmap = Object.fromEntries(customers.map(c=>[c.id,c]));
  let list = [...recs].sort((a,b)=>(b.receivedDate||b.createdAt||'')>(a.receivedDate||a.createdAt||'')?1:-1);
  if(filterText){
    list = list.filter(r=>{
      const e=emap[r.equipmentId]; const c=e?cmap[e.customerId]:null;
      return fuzzyMatch(filterText, c?.name, e?.modelNo, r.mgmtNo);
    });
  }
  const tbody = document.getElementById('copy-picker-list');
  tbody.innerHTML = list.length===0
    ? `<tr><td colspan="5" class="text-center" style="padding:24px;color:var(--text3)">該当する記録がありません</td></tr>`
    : list.map(r=>{
        const e=emap[r.equipmentId]; const c=e?cmap[e.customerId]:null;
        return `<tr>
          <td>${fmtDate(r.receivedDate)}</td>
          <td>${escHtml(r.mgmtNo||'')}</td>
          <td>${c?escHtml(c.name):'—'} / ${e?escHtml(e.modelNo||''):'—'}</td>
          <td>${escHtml(r.workType||'')}</td>
          <td><button class="btn btn-xs btn-primary" type="button" onclick="copyAndIssue(${r.id})">この内容でコピー発行</button></td>
        </tr>`;
      }).join('');
}
```

- [ ] **Step 5: コピー発行ロジックを実装**

過去記録から **機器（equipmentId）と作業種別（workType）のみ引き継ぎ**、その他はクリアして新規依頼モーダルを開く（依頼元・住所・ハウスメーカーは equipmentId に紐づく顧客から自動的に同伴するため、複製不要）。保存は通常の `saveRecord` を使う。

```js
async function copyAndIssue(srcId){
  const src = await dbGet('serviceRecords', srcId);
  if(!src){ alert('元の記録が見つかりません'); return; }
  closeModal('copy-picker-modal');
  await openRecordModal();                 // 新規モード（id空・管理番号自動採番）
  document.getElementById('record-modal-title').textContent = '新規依頼発行（コピー）';
  // 引き継ぐ項目
  document.getElementById('record-equipmentId').value = src.equipmentId || '';
  document.getElementById('record-workType').value = src.workType || '定期点検';
  // 新規でクリアする項目（コール内容・対応日・完了日・担当者・所見・部品・料金）
  document.getElementById('record-receivedDate').value = today();
  ['requestedDate','serviceDate','completionDate','staff','receptionist'].forEach(f=>{
    document.getElementById('record-'+f).value = '';
  });
  ['callContent','remarks','customerReport'].forEach(f=>{
    document.getElementById('record-'+f).value = '';
  });
  document.getElementById('record-visitFee').value = '';
  document.getElementById('record-laborFee').value = '';
  _partRows = [];
  renderParts();
}
```

- [ ] **Step 6: ピッカー検索イベントを登録**

`DOMContentLoaded` 末尾に追加：

```js
document.getElementById('copy-picker-search').addEventListener('input', e=>renderCopyPicker(e.target.value));
```

- [ ] **Step 7: ブラウザで確認**

新規依頼発行タブ →「新規依頼を起こす」→ 受付日が今日で入る・管理番号自動。機器を選び作業種別「故障修理」、コール内容を入力して（完了日は空のまま）保存。
Expected: 「未完了の依頼」一覧に1件表示。サービス記録タブでも「進行中」で見える。

次にコピー発行を確認：完了済み記録（部品・料金あり）を1件用意 →「過去履歴からコピーして発行」→ 一覧から「この内容でコピー発行」。
Expected: 機器と作業種別が引き継がれ、管理番号は新規・受付日は今日・コール内容/部品/料金/担当者/完了日は空。検索欄で顧客名や型式（半角全角ゆれOK）で絞り込める。保存すると未完了の依頼に新規1件が追加される（元記録は変化しない）。依頼票印刷でお客様・依頼元・ハウスメーカーが正しく出る。

- [ ] **Step 8: コミット**

```bash
git add "Service Book/index.html" "Service Book/js/app.js"
git commit -m "feat(service-book): new-request intake with copy-from-history"
```

---

## Task 8: 依頼票（作業指示票）A4印刷

**Files:**
- Modify: `Service Book/js/app.js`
- Modify: `Service Book/index.html`（印刷ページ・印刷CSS）
- Modify: `Service Book/css/style.css`（`@media print`）

- [ ] **Step 1: 印刷ページの器をindex.htmlに追加**

`#page-print` の中身：

```html
<div class="page-head no-print">
  <button class="btn btn-secondary" type="button" onclick="navigateTo('records')">← 戻る</button>
  <button class="btn btn-primary" type="button" onclick="window.print()">印刷 / PDF</button>
</div>
<div id="print-area" class="print-sheet"></div>
```

- [ ] **Step 2: 依頼票レンダラを実装**

```js
async function loadRecordWithRefs(id){
  const r = await dbGet('serviceRecords', id);
  if(!r) return null;
  const e = await dbGet('equipments', r.equipmentId);
  const c = e ? await dbGet('customers', e.customerId) : null;
  return { r, e, c };
}

async function openRequestSlip(id){
  const ref = await loadRecordWithRefs(id);
  if(!ref){ alert('記録が見つかりません'); return; }
  const {r,e,c} = ref;
  document.getElementById('print-area').innerHTML = `
    <div class="sheet-header"><h1>作業依頼票</h1>
      <div class="sheet-meta">管理番号: ${escHtml(r.mgmtNo||'—')}<br>受付日: ${fmtDate(r.receivedDate)}</div></div>
    <table class="sheet-table">
      <tr><th>受付者</th><td>${escHtml(r.receptionist||'')}</td><th>希望日</th><td>${fmtDate(r.requestedDate)}</td></tr>
      <tr><th>作業種別</th><td colspan="3">${escHtml(r.workType||'')}</td></tr>
      <tr><th>依頼元 / 販売店</th><td colspan="3">${escHtml(c?.dealerName||'')}</td></tr>
      <tr><th>お客様</th><td colspan="3">${escHtml(c?.name||'')}（${escHtml(c?.houseMaker||'')}）</td></tr>
      <tr><th>住所</th><td colspan="3">${escHtml(c?.address||'')}</td></tr>
      <tr><th>連絡先</th><td colspan="3">${escHtml(c?.contact||'')}</td></tr>
      ${c?.printNote?`<tr><th>特記事項</th><td colspan="3">${escHtml(c.printNote)}</td></tr>`:''}
      <tr><th>機種</th><td colspan="3">${escHtml(e?.maker||'')} ${escHtml(e?.modelNo||'')} / ロット:${escHtml(e?.lotNo||'')} / 用途:${escHtml(e?.usage||'')}</td></tr>
      <tr><th>コール内容</th><td colspan="3" class="cell-multiline">${escHtml(r.callContent||'')}</td></tr>
    </table>
    <h3>現場記入欄</h3>
    <table class="sheet-table blank">
      <tr><th>対応日</th><td></td><th>担当者</th><td></td></tr>
      <tr><th>作業内容</th><td colspan="3" style="height:120px"></td></tr>
    </table>
    <div class="sheet-footer">© 2026 Nozomi Sakurada</div>`;
  navigateTo('print');
}
```

- [ ] **Step 3: 印刷CSSをstyle.cssに追加**

```css
.print-sheet{ background:#fff; max-width:210mm; margin:0 auto; padding:16mm; }
.sheet-header{ display:flex; justify-content:space-between; align-items:flex-start; border-bottom:2px solid #222; padding-bottom:8px; }
.sheet-header h1{ font-size:22px; margin:0; }
.sheet-meta{ text-align:right; font-size:12px; }
.sheet-table{ width:100%; border-collapse:collapse; margin-top:12px; font-size:12px; }
.sheet-table th,.sheet-table td{ border:1px solid #999; padding:6px 8px; text-align:left; vertical-align:top; }
.sheet-table th{ background:#f3f3f3; width:120px; white-space:nowrap; }
.sheet-table .cell-multiline{ white-space:pre-wrap; }
.sheet-table.blank td{ height:32px; }
.sheet-footer{ margin-top:16px; font-size:10px; color:#888; text-align:right; }
@media print{
  .app-header,.nav,.no-print,#toast{ display:none !important; }
  .page{ display:none !important; }
  #page-print{ display:block !important; }
  .print-sheet{ padding:0; max-width:none; }
  @page{ size:A4 portrait; margin:14mm; }
}
```

- [ ] **Step 4: ブラウザで確認**

依頼タブの未完了一覧で「依頼票」を押す → 印刷ページに作業依頼票が表示 →「印刷 / PDF」で Ctrl+P プレビュー。
Expected: A4縦1枚に、受付情報・お客様（ハウスメーカー併記）・特記事項・機種・コール内容・現場記入空欄が収まる。ヘッダー/ナビ/ボタンは印刷に出ない。

- [ ] **Step 5: コミット**

```bash
git add "Service Book/index.html" "Service Book/js/app.js" "Service Book/css/style.css"
git commit -m "feat(service-book): A4 work-request slip print"
```

---

## Task 9: サービス報告書 A4印刷

**Files:**
- Modify: `Service Book/js/app.js`

- [ ] **Step 1: 報告書レンダラを実装**

```js
async function openPrint(id){
  const ref = await loadRecordWithRefs(id);
  if(!ref){ alert('記録が見つかりません'); return; }
  const {r,e,c} = ref;
  const partsRows = (r.parts||[]).map(p=>`<tr>
      <td>${escHtml(p.name||'')}</td><td class="text-center">${p.qty||0}</td>
      <td class="text-right">${fmtYen(p.unitPrice)}</td>
      <td class="text-right">${fmtYen((p.qty||0)*(p.unitPrice||0))}</td></tr>`).join('')
    || `<tr><td colspan="4" class="text-center" style="color:#999">交換部品なし</td></tr>`;
  const fee = r.fee||{visitFee:0,laborFee:0,partsFee:0,total:0};
  document.getElementById('print-area').innerHTML = `
    <div class="sheet-header"><h1>サービス報告書</h1>
      <div class="sheet-meta">管理番号: ${escHtml(r.mgmtNo||'—')}<br>対応日: ${fmtDate(r.serviceDate)}<br>完了日: ${fmtDate(r.completionDate)}</div></div>
    <table class="sheet-table">
      <tr><th>依頼元 / 販売店</th><td colspan="3">${escHtml(c?.dealerName||'')}</td></tr>
      <tr><th>お客様</th><td colspan="3">${escHtml(c?.name||'')}（${escHtml(c?.houseMaker||'')}）</td></tr>
      <tr><th>住所</th><td>${escHtml(c?.address||'')}</td><th>連絡先</th><td>${escHtml(c?.contact||'')}</td></tr>
      ${c?.printNote?`<tr><th>特記事項</th><td colspan="3">${escHtml(c.printNote)}</td></tr>`:''}
      <tr><th>機種</th><td colspan="3">${escHtml(e?.maker||'')} ${escHtml(e?.modelNo||'')} / ロット:${escHtml(e?.lotNo||'')} / 用途:${escHtml(e?.usage||'')}</td></tr>
      <tr><th>作業種別</th><td>${escHtml(r.workType||'')}</td><th>担当者</th><td>${escHtml(r.staff||'')}</td></tr>
      <tr><th>コール内容</th><td colspan="3" class="cell-multiline">${escHtml(r.callContent||'')}</td></tr>
      <tr><th>担当者所見</th><td colspan="3" class="cell-multiline">${escHtml(r.remarks||'')}</td></tr>
      <tr><th>お客様への報告</th><td colspan="3" class="cell-multiline">${escHtml(r.customerReport||'')}</td></tr>
    </table>
    <h3>交換部品</h3>
    <table class="sheet-table"><thead><tr><th>品名</th><th>数量</th><th>単価</th><th>金額</th></tr></thead><tbody>${partsRows}</tbody></table>
    <h3>料金明細</h3>
    <table class="sheet-table">
      <tr><th>出張料</th><td class="text-right">${fmtYen(fee.visitFee)}</td>
          <th>技術料</th><td class="text-right">${fmtYen(fee.laborFee)}</td></tr>
      <tr><th>部品代</th><td class="text-right">${fmtYen(fee.partsFee)}</td>
          <th>合計</th><td class="text-right"><strong>${fmtYen(fee.total)}</strong></td></tr>
    </table>
    <table class="sheet-table blank" style="margin-top:20px">
      <tr><th style="width:140px">お客様確認サイン</th><td style="height:60px"></td></tr>
    </table>
    <div class="sheet-footer">© 2026 Nozomi Sakurada. All rights reserved.</div>`;
  navigateTo('print');
}
```

- [ ] **Step 2: ブラウザで確認**

完了済み記録（部品・料金あり）で「報告書」を押す。
Expected: A4縦に、依頼元・お客様（ハウスメーカー）・特記事項・機種・コール内容・所見・お客様報告・交換部品テーブル・料金明細（合計一致）・サイン欄が表示。`memo`（備考）は出力されない。Ctrl+P で1ページに収まる。

- [ ] **Step 3: コミット**

```bash
git add "Service Book/js/app.js"
git commit -m "feat(service-book): A4 service report print"
```

---

## Task 10: ポータル登録

**Files:**
- Modify: `js/tools.js`（ポータル直下の共通ファイル）

- [ ] **Step 1: PORTAL_TOOLS にエントリを追加**

[../js/tools.js](../js/tools.js) の配列末尾（`btob_price` の後）に追加：

```js
,{
    id: "service_book",
    title: "サービス報告書・点検履歴台帳",
    desc: "顧客・機器ごとに点検/修理履歴を蓄積。フォルダ同期、横断あいまい検索、依頼票・サービス報告書のA4発行",
    category: "maintenance",
    tags: ["点検履歴", "サービス報告書", "台帳"],
    url: "Service Book/index.html",
    icon: "clipboard-list"
}
```

- [ ] **Step 2: ブラウザで確認**

ポータルの [../index.html](../index.html) を開く。
Expected: メンテナンスカテゴリに「サービス報告書・点検履歴台帳」カードが表示され、クリックで `Service Book/index.html` に遷移する。

- [ ] **Step 3: コミット**

```bash
git add "js/tools.js"
git commit -m "feat(portal): register Service Book tool"
```

---

## 自己レビュー結果（spec照合）

- **保存方式（フォルダ同期）** → Task 3 ✅
- **3階層（顧客/機器/記録）** → Task 2 ストア定義 + Task 4/5/6 ✅
- **横断あいまい検索＋半角全角自動変換** → Task 1（util）+ Task 4/5 で適用 ✅
- **新規依頼発行＋依頼票印刷** → Task 7 + Task 8 ✅
- **過去履歴からコピーして依頼発行（機器・作業種別を引き継ぎ、依頼元等は顧客経由で同伴）** → Task 7 Step 4–7 ✅
- **サービス報告書（依頼元/お客様/機種/ロット/完了日/所見/コール内容/部品/料金）** → Task 6（入力）+ Task 9（出力）✅
- **用途（usage）** → Task 5 / Task 8・9 ✅
- **ハウスメーカー・特記事項(印刷)・備考(非印刷)** → Task 4 入力 / Task 8・9 で printNote のみ印刷・memo 除外 ✅
- **放置案件警告（未完了・14日）** → Task 6 `staleDaysOf` + バッジ/赤強調 ✅
- **ポータル登録** → Task 10 ✅
- **スコープ外（写真・カレンダー通知・履歴一覧PDF）** → 未実装で意図どおり ✅

型整合: `loadRecordWithRefs`（Task 9）は Task 8 で定義 → 順序上 Task 8 が先に定義するため OK。`onCustomersChanged/onEquipmentsChanged/onRecordsChanged`（Task 3 定義）を Task 4/5/6 で使用 → 整合。`nextMgmtNo`（Task 6 定義）を Task 7 が利用 → Task 6 が先のため OK。
